# ============================================
# EXEMPLO: Arquivo de Configuração do Backend
# ============================================
# Copie este arquivo para .env e preencha com valores reais
# NUNCA commite o arquivo .env no Git!

# Servidor
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/myia

# JWT
JWT_SECRET=generate_with_openssl_rand_base64_32
JWT_EXPIRES_IN=7d

# Encryption
ENCRYPTION_SECRET=generate_with_openssl_rand_base64_32

# CORS
CORS_ORIGIN=http://localhost:3000,http://localhost:3003

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Chat / Context Configuration
MAX_CONTEXT_MESSAGES=15
CONTEXT_CLEANUP_INTERVAL=3600000

# Default AI Provider
API_PROVIDER=groq

# OpenAI
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-3.5-turbo

# Groq
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instant

# Azure OpenAI (RAG - Embeddings)
AZURE_EMBEDDING_ENDPOINT=https://your-resource.openai.azure.com
AZURE_API_KEY=your_azure_api_key_here
AZURE_EMBEDDING_DEPLOYMENT_NAME=text-embedding-3-small

# AWS Bedrock
AWS_BEDROCK_CREDENTIALS=ACCESS_KEY:SECRET_KEY
AWS_BEDROCK_REGION=us-east-1

# Feature Flags
USE_NEW_ADAPTERS=true

# OAuth / Third-party
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret
GITHUB_OAUTH_CALLBACK_URL=http://localhost:3001/api/auth/github/callback

# PostgreSQL Transport para logs (Grafana)
ENABLE_POSTGRES_TRANSPORT=true

#############################################
# Observações / Boas práticas
#############################################
# - NÃO coloque chaves reais neste arquivo no repositório.
# - Use um arquivo local .env (gitignored) ou um secret manager em produção.
# - Gere segredos com: openssl rand -base64 32
# - Variáveis opcionais podem ser removidas se não forem usadas pelo seu provedor.
